# Database (Fly.io会自动设置)
DATABASE_URL=postgres://user:password@host:5432/dbname

# JWT Secret (生成一个强密码)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Frontend URL (生产环境的前端地址)
FRONTEND_URL=https://your-app.fly.dev

# Node Environment
NODE_ENV=production

# Port
PORT=3000